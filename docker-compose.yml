services:
  myapp2:
    build:
      dockerfile: Dockerfile
      context: ./client
    container_name: myapp2_frontend_container
    hostname: myapp2_frontend_host
    env_file:
      - ./client/.env
    ports:
      - '3020:5173'
    restart: always

networks:
  default:
    name: nginx_rp_tier
    external: true
